pipeline {
    agent any
    stages {
        stage('maven clean') {
            steps {
                sh "echo maven"
                sh "mvn clean"
            }
        }
        stage('maven test') {
            steps {
                sh "mvn test"
            }
        }
        stage('maven deploy') {
            steps {
                sh "mvn package"
            }
        }
    }
}
